# Dependency table moved to pyproject.toml. This file is kept so that
# `pip install -r requirements.txt` still works as a familiar entry point;
# `-e .` resolves to the [project].dependencies in pyproject.toml.
-e .
